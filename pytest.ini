[pytest]
addopts = -m "not e2e and not performance"
markers =
    e2e: end-to-end tests requiring real infrastructure (Redis, MongoDB, Milvus)
    performance: slow latency/throughput tests — run explicitly with -m performance
asyncio_mode = strict
