# ============================================================
# ERP Agentic RAG — Python dependencies
# Generated: 2026-04-21  (python -m pip freeze > requirements.txt)
# Virtualenv: erp-rag-env-v2
#
# Sections:
#   [frozen]   — exact versions from the active virtualenv
#   [sprint-6] — packages required to complete Sprint 6 tasks
# ============================================================

# ── Web framework ────────────────────────────────────────────
fastapi==0.135.3
starlette==1.0.0
uvicorn>=0.34.0             # ASGI server (add when wiring main.py)

# ── Task queue ───────────────────────────────────────────────
celery==5.6.3
kombu==5.6.2
billiard==4.2.4
amqp==5.3.1
vine==5.1.0
redis==6.4.0                # broker + result backend

# ── Data / validation ────────────────────────────────────────
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
jsonschema==4.26.0          # PromptRegistry validates LLM output against JSON schemas
jsonschema-specifications==2025.9.1
python-multipart==0.0.26    # required by POST /api/assets/upload (multipart form)

# ── Auth ─────────────────────────────────────────────────────
PyJWT==2.12.1
passlib==1.7.4
bcrypt==5.0.0
cryptography==46.0.7

# ── Observability ────────────────────────────────────────────
prometheus_client==0.24.1

# ── LLM providers ────────────────────────────────────────────
google-genai==1.73.1        # GeminiLLMClient (primary LLM since Sprint 8)

# ── Configuration ────────────────────────────────────────────
python-dotenv==1.2.2        # loads .env for the app and worker entrypoints

# ── LangChain (text splitting) ───────────────────────────────
langchain-core==1.3.0
langchain-text-splitters==1.1.2
langsmith==0.7.32

# ── HTTP / networking ────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
requests==2.33.1
requests-toolbelt==1.0.0
certifi==2026.2.25
urllib3==2.6.3
idna==3.11
charset-normalizer==3.4.7
h11==0.16.0
anyio==4.13.0

# ── Serialisation / utilities ────────────────────────────────
orjson==3.11.8
PyYAML==6.0.3
python-dateutil==2.9.0.post0
pytz==2026.1.post1
tzdata==2026.1
tzlocal==5.3.1
six==1.17.0
jsonpatch==1.33
jsonpointer==3.1.1
xxhash==3.6.0
zstandard==0.25.0
uuid_utils==0.14.1

# ── CLI / REPL deps (Celery shell) ───────────────────────────
click==8.3.2
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
prompt_toolkit==3.0.52
wcwidth==0.6.0

# ── Misc stdlib helpers ──────────────────────────────────────
annotated-doc==0.0.4
cffi==2.0.0
pycparser==3.0
librt==0.8.1
sentinels==1.1.1
tenacity==9.1.4
typing-inspection==0.4.2
typing_extensions==4.15.0
packaging==26.0
pathspec==1.0.4
Pygments==2.20.0

# ── Testing ──────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
coverage==7.13.5
mongomock==4.3.0            # in-memory MongoDB for unit tests

# ── Linting / type checking ──────────────────────────────────
ruff==0.15.9
mypy==1.20.0
mypy_extensions==1.1.0
types-PyYAML==6.0.12.20250915   # mypy stubs for PromptRegistry's yaml loader

# ============================================================
# Sprint 6 — Worker Reliability (new / missing dependencies)
# ============================================================

# Task 2 — MongoDB dead-letter + idempotency store (production)
pymongo==4.17.0

# Task 3 — TaxCircularChunker: layout analysis (text + table separation)
# Base package covers partition_text and element detection for plain text.
# For PDF/DOCX layout (future sprints), add: unstructured[pdf] or unstructured[docx]
unstructured==0.22.22

# Task 4 — embed_task: vector store client (idempotent embedding, no duplicate vectors)
pymilvus==2.5.18
setuptools>=68,<71          # milvus-lite depends on pkg_resources (removed in Python 3.12+)
